# Environment: mediapipe_env
# Runs: mediapipe_server.py (pose analysis HTTP service, port 8001)
#
# Kept in a separate venv from main_env/deepface_env because mediapipe
# pins its own numpy/protobuf-compatible versions that have historically
# conflicted with insightface/onnxruntime (main_env) and deepface's
# TensorFlow stack (deepface_env).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
mediapipe>=0.10
opencv-python>=4.9
numpy>=1.26
